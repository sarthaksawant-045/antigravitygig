# Default to the hosted backend so the app works even when Flask is not running locally.
VITE_API_BASE_URL=https://antigravitygig-2.onrender.com

# For local backend development, replace the line above with:
# VITE_API_BASE_URL=http://127.0.0.1:5000
